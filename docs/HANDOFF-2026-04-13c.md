# HANDOFF 2026-04-13c — Post-cleanup state

Use this together with `docs/HANDOFF-2026-04-13.md` and
`docs/HANDOFF-2026-04-13b.md`.

## Current state

The normal Maschine-open bridge workflow is still the authoritative path.
Ignore the standalone raw USB probing branch unless there is a specific reason
to reopen it.

Resolved in code and validated in the current workflow:

- scene-row Packet A routing is in place for Mute, Solo, Select, Duplicate,
  Navigate, Pad Mode, Pattern, Scene, Shift, Erase, Grid, TransportLeft,
  Record, and Play
- pad rubber LEDs are mapped through Packet A slots `0..15`
- Restart is handled as `logical[31] -> phys[2]`
- Note Repeat is handled as extended `logical[56] -> phys[27]`

Still unresolved:

- TransportRight: software signal is `logical[27]`, but there is still no
  confirmed visible hardware LED slot for it, so the bridge suppresses it by
  default to avoid lighting the wrong LED

## Concrete physical labels currently encoded

Packet B physical labels present in the code:

- `phys[1]` Transport Left
- `phys[2]` Restart
- `phys[3]` Group H
- `phys[4]` Group G
- `phys[5]` Group D
- `phys[6]` Group C
- `phys[7]` Group F
- `phys[8]` Group E
- `phys[9]` Group B
- `phys[10]` Group A
- `phys[11]` Auto Write
- `phys[12]` Snap
- `phys[13]` Modules Right
- `phys[14]` Modules Left
- `phys[15]` Sampling
- `phys[16]` Browse
- `phys[17]` Step
- `phys[18]` Control
- `phys[19]` SA8
- `phys[20]` SA7
- `phys[21]` SA6
- `phys[22]` SA5
- `phys[23]` SA4
- `phys[24]` SA3
- `phys[25]` SA2
- `phys[26]` SA1
- `phys[27]` Note Repeat

Evidence strength in comments:

- `phys[17..27]` are treated as stable
- `phys[1..16]` are still tagged provisional in the table even though several
  were visually checked during this session
- Mute and Shift remain coded but still described as assumed sequential

## Recommended next step after commit

Test the current bridge as-is in the normal Maschine workflow before making more
mapping changes.

If the test matches expectations, the next remaining LED task is only
TransportRight. The most honest current behavior is to keep it suppressed until
we can prove a visible destination.
