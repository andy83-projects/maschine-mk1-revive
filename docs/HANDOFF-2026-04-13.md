# HANDOFF 2026-04-13 — LED transport mapping + scene-row complete

## What was accomplished this session

### Scene-row (PADS area) buttons — COMPLETE
All 8 PADS-area buttons are now routed through Packet A and confirmed working on hardware:

| Button | Logical | A-pkt slot | Status |
|---|---|---|---|
| Mute | [16] | 16 | assumed (sequential) |
| Solo | [17] | 17 | confirmed |
| Select | [18] | 18 | confirmed |
| Duplicate | [19] | 19 | confirmed |
| Navigate | [20] | 20 | confirmed |
| Pad Mode | [21] | 21 | confirmed |
| Pattern | [22] | 22 | confirmed |
| Scene | [23] | 23 | confirmed |

### Transport area buttons — mostly complete

| Button | Logical | Routing | Status |
|---|---|---|---|
| Shift | [24] | A-pkt slot 24 | confirmed |
| Erase | [25] | A-pkt slot 25 | confirmed |
| Grid | [26] | A-pkt slot 26 | confirmed |
| TransportLeft | [30] | A-pkt slot 27 | confirmed — hardware LED lights |
| **TransportRight** | **[27]** | **suppressed (no slot yet)** | **INCOMPLETE — see below** |
| Record | [28] | A-pkt slot 28 | confirmed |
| Play | [29] | A-pkt slot 29 | confirmed (0x3f when armed) |

### Protocol reminder

Two EP1 packets per LED update:
- **Packet A** `{0x0c, 0x00, data[0..30]}` — pad rubbers (slots 0–15) + scene-row (slots 16–29)
- **Packet B** `{0x0c, 0x1e, data[0..32]}` — group buttons, SA1–SA8, Note Repeat, etc.

---

## Open issue: TransportRight LED slot unknown

**What we know:**
- logical[27] = TransportRight (confirmed from targeted transport learn 2026-04-13)
- Before this session logical[27] was passthrough in B-pkt → was driving Note Repeat hardware LED (wrong)
- logical[27] is now **suppressed** from B-pkt in `k_apple_upper` (fix: `{27, 0xFE, ...}`)
- TransportRight has no confirmed A-pkt slot in the probe table
- A-pkt **slot 30 is labeled "unused"** — prime candidate for TransportRight

**Next step: probe slot 30**
```sh
MK1_LED_PROBE_OFFSET=0 \
MK1_LED_PROBE_FROM=30 \
MK1_LED_PROBE_TO=30 \
MK1_LED_PROBE_INTERACTIVE=1 \
./build/Debug/mk1-bridge
```
Watch which hardware LED lights. Expected: TransportRight button LED.

If slot 30 = TransportRight, add to `k_scene_row[]` in `forward_led()`:
```c
{ 27, 30 },  // TransportRight (confirmed)
```
And add to `k_apple_upper`:
```c
{ 27, 0xFE, "TransportRight (scene-row)" },
```
(change the existing suppress entry — it's already 0xFE, just update the comment)

---

## Open issue: Note Repeat logical index unknown

Note Repeat is in the Master area. Its hardware LED is at B-pkt data[27] = device pos 57.
It is driven by `k_hw_by_logical[27] = 27 → remapped[27]` in the linear remap.
But logical[27] = TransportRight, so Note Repeat has no known logical index.

**Next step:** run a targeted learn with Note Repeat in the sequence. Note Repeat is likely
at a logical index > 31 (outside the currently-mapped B-pkt range) or at an index we've
been suppressing. Candidates: look at any unresolved `logical[N]=0x??` lines in led.log
when Note Repeat is pressed.

---

## Open issue: Restart logical index unclear

From learn 2026-04-13:
- Restart arm: `logical[31] 0x13→0x32` (also logical[29] went 0x00→0x32 — Play arming from Restart?)
- Restart disarm: `logical[31] 0x32→0x13`

`logical[31]` appears to be the Restart indicator. Currently it goes to B-pkt data[31] = device pos 61.
The B-pkt probe labeled data[4] = Restart (device pos 34), driven by `k_hw_by_logical[1] = 4`.
But `logical[1]` is suppressed in `k_apple_upper` (`{1, 0xFE, "log1"}`).

**What to check:** run a targeted probe at B-pkt slot 4 (set `remapped[4]`) to confirm which
hardware LED it lights. Also consider whether logical[31] should be redirected to B-pkt data[4].

---

## Current k_scene_row in forward_led()

```c
static const struct { uint8_t log_idx; uint8_t a_slot; } k_scene_row[] = {
    { 16, 16 },  // Mute          (assumed)
    { 17, 17 },  // Solo          (confirmed)
    { 18, 18 },  // Select        (confirmed)
    { 19, 19 },  // Duplicate     (confirmed)
    { 20, 20 },  // Navigate      (confirmed)
    { 21, 21 },  // Pad Mode      (confirmed)
    { 22, 22 },  // Pattern       (confirmed)
    { 23, 23 },  // Scene         (confirmed)
    { 24, 24 },  // Shift         (confirmed)
    { 25, 25 },  // Erase         (confirmed)
    { 26, 26 },  // Grid          (confirmed)
    { 30, 27 },  // TransportLeft (confirmed; logical[30] → slot 27)
    { 28, 28 },  // Record        (confirmed)
    { 29, 29 },  // Play          (confirmed)
    // TransportRight (logical[27]) → slot TBD; suppressed in k_apple_upper until confirmed
};
```

---

## What hardware LEDs are visually confirmed working

From user observation 2026-04-13:
- Pad rubber LEDs: working correctly
- Shift, Play, Record, Grid: correct LEDs light
- TransportLeft: correct LED lights
- PADS row (Mute–Scene): LEDs routing correctly (Mute unconfirmed by eye, assumed)

---

## Build

```sh
xcodebuild -project maschine-mk1-revive.xcodeproj -scheme mk1-bridge -configuration Debug build
```
No errors. One pre-existing warning: `run_led_verify` format string at ~line 1968 (missing arg for `startup_delay_ms`).
