# HANDOFF 2026-04-13b — Return to Maschine-open mapping workflow

## Decision for next chat

Do **not** continue the standalone USB-probe side quest unless it becomes
necessary later.

The successful work this project made today was done with the normal
`mk1-bridge` + Maschine-open workflow, and that remains the recommended path.

Use this handoff together with `docs/HANDOFF-2026-04-13.md`.

---

## What remains authoritative

The core mapping progress from `HANDOFF-2026-04-13.md` still stands:

- PADS-area Scene..Mute row is effectively correct in Packet A
- transport buttons confirmed so far:
  - Shift `[24]`
  - Erase `[25]`
  - Grid `[26]`
  - TransportLeft `[30] -> A slot 27`
  - Record `[28]`
  - Play `[29]`
- `logical[27] = TransportRight` is still the current conclusion
- `logical[27]` is correctly suppressed from B-pkt so it no longer lights
  Note Repeat incorrectly

The unresolved items are now:

1. TransportRight visible slot

---

## What was tried and why it is not the main path

This session tried to confirm `TransportRight` by direct standalone LED probing
 without Maschine running.

Reason:

- direct raw slot probe would have been a clean way to answer:
  “does Packet A slot 30 light TransportRight?”

What happened:

- standalone probe mode was added to `mk1-bridge/main.c`
- the build succeeded
- IPC conflicts were bypassed
- but raw USB open still failed repeatedly at:
  `IOCreatePlugInInterfaceForService(device) failed: 0xe00002be`

Important:

- this does **not** invalidate the normal bridge workflow
- this does **not** invalidate the mapping work already completed with Maschine
  open
- this is a side issue affecting standalone raw USB probing only

Conclusion:

- go back to the original method that was already producing results:
  Maschine open, bridge connected, targeted learn / targeted runtime checks

---

## Recommended next steps

### 1. Resume normal bridge workflow with Maschine open

Do not lead with standalone probe mode.

Open Maschine, run the bridge normally, and continue targeted confirmation using
the existing learn/runtime method that already worked.

### 2. Finish TransportRight first

Goal:

- confirm the visible `TransportRight` LED slot in the normal bridge workflow

Current evidence update:

- local testing now also says the Packet B candidate sweep did not show any
  visible TransportRight LED
- local `build/Debug/bridge-logs/led.log` already contains guided probe notes for
  Packet A slot `30` recorded as `none` / `nothing`
- that means neither the old "A slot 30" theory nor the tested Packet B
  candidates should be treated as established

Preferred approach for next chat:

- keep Maschine open
- test the current bridge state first, then only continue mapping if needed
- if TransportRight still matters after that, keep using filtered learn / guided
  verify in the normal bridge workflow instead of standalone raw USB probing

```sh
MK1_LED_LEARN=1 \
MK1_LED_LEARN_FILTER='TransportRight' \
MK1_LED_LEARN_REPEATS=2 \
./build/Debug/mk1-bridge
```

- use that focused runtime evidence only if new behavior appears that suggests a
  real visible destination; otherwise keep it suppressed

### 3. Then do Note Repeat learn

Resolved later in follow-up work and should stay documented as resolved:

- `Note Repeat` is now routed from extended `logical[56]` to `phys[27]`
- visual hardware check reports that `Note Repeat` now looks correct

### 4. Restart status

Restart is no longer part of the unresolved set:

- `logical[31]` is the restart-specific LED signal
- `logical[29]` remains `Play`
- restart is routed to the visible Restart LED in the current bridge code
- observed behavior is dim at startup and bright while pressed

---

## Current local code state

There are uncommitted local edits in `mk1-bridge/main.c` from the standalone
probe investigation plus the normal-workflow mapping fixes from this session.

These edits include:

- allow guided LED probe / verify modes to run without IPC
- add extra open retries in guided standalone mode
- add filtered/repeated learn support
- add normal-bridge guided verify support
- add Restart routing and Note Repeat extended routing
- add runtime TransportRight candidate overrides for Packet A / Packet B testing

The normal IPC bridge path remains the main path and should be tested first.

If the next chat wants to strip out standalone-only work later, do that as a
separate cleanup after verifying the current bridge behavior.

Also saved locally during this session:

- `/tmp/mk1-guided-probe-standalone.patch`

That patch was created as a rollback artifact for the earlier standalone-probe
changes.

---

## Suggested opening prompt for next chat

Use something close to:

> Continue from `docs/HANDOFF-2026-04-13.md` and `docs/HANDOFF-2026-04-13b.md`.
> Ignore standalone USB probing for now. Return to the normal Maschine-open
> bridge workflow. Treat Restart and Note Repeat as resolved, test the current
> mapping state first, and only continue on TransportRight if it still appears
> to be missing.
