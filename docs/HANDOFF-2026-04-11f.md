# MK1 Project LED Capture Handoff — 2026-04-11f

## Scope

Follow-on session after `docs/HANDOFF-2026-04-11e.md`.

This pass had two goals:

- verify that the `phys[0]` control-byte overwrite was actually fixed in the `Debug` build
- resolve the remaining `Mute` / `Solo` ambiguity with a focused capture pass

## Main Outcome

Both goals were met.

- the rebuilt `Debug` binary no longer contaminates project-capture output with `phys[0]`
- focused `Mute` / `Solo` capture resolved the remaining pad-section ambiguity

## Verified Capture State

The first rerun after rebuilding `Debug` showed two distinct capture blocks:

- an older stale run still showed `phys[0]` churn
- the later fresh run did not show any `phys[0]` changes in the `phys:` diffs

That confirms the `logical[0]` -> `phys[0]` overwrite path was fixed in the binary
actually being used from `build/Debug`.

## Clean Mapping Evidence

From the clean post-fix sequence:

- `Pad Mode` -> `phys[21]`
- `Navigate` -> `phys[20]`
- `Select` -> `phys[18]`

Those three remain stable.

## Focused Mute / Solo Evidence

User-provided focused `PROJCAP` lines:

```text
15:[led]     PROJCAP baseline: seq=1 logical_len=57 phys_len=33
134:[led]     PROJCAP [1] mute logical: logical[0]:00->13 logical[1]:00->13 logical[2]:00->13 logical[3]:00->32 logical[6]:00->13 logical[7]:00->13 logical[16]:00->32 logical[31]:00->13 logical[34]:00->13 logical[35]:00->13 logical[36]:00->13 logical[37]:00->13 logical[38]:00->13 logical[39]:00->32 logical[50]:32->13 logical[52]:13->00 ...
135:[led]     PROJCAP [1] mute phys: phys[17]:00->32 phys[31]:00->13
211:[led]     PROJCAP [2] solo logical: logical[16]:32->00 logical[17]:00->32
212:[led]     PROJCAP [2] solo phys: phys[17]:32->00 phys[23]:00->32
```

This is the strongest current evidence for the remaining two buttons:

- `Mute` lights `phys[17]`
- `Solo` lights `phys[23]`

The `solo` step is especially useful because it shows a clean handoff:

- `logical[16]:32->00` and `phys[17]:32->00`
- `logical[17]:00->32` and `phys[23]:00->32`

That makes the current best carrier pairing:

- `logical[16]` -> `phys[17]` = `Mute`
- `logical[17]` -> `phys[23]` = `Solo`

## Current Best Mapping

The best current Apple Silicon pad-section mapping is now:

- `Mute` -> `phys[17]`
- `Select` -> `phys[18]`
- `Navigate` -> `phys[20]`
- `Pad Mode` -> `phys[21]`
- `Solo` -> `phys[23]`

Still unresolved:

- `Pattern`
- `Duplicate`
- `Scene` adjacency questions in the upper range
- any hidden upper-slot state in `logical[53..55]`

## Capture Caveat

The user reports an ongoing capture-mode quirk:

- the first button press is sometimes not recognized

This was treated as an existing project-capture behavior, not as a regression from the
`phys[0]` fix, and it was not changed in this session.

## Code Changes In This Session

- `mk1-bridge/main.c`

Applied changes:

- preserved `phys[0] = 0x1e` by skipping writes to physical slot 0 in the linear remap loop
- kept project capture alive across initial baseline timeouts instead of exiting
- encoded the newly confirmed Apple Silicon redirects:
  - `logical[16] -> phys[17]` for `Mute`
  - `logical[17] -> phys[23]` for `Solo`

## Recommended Next Step

Use the now-clean mapping to continue deliberate Apple Silicon remap work.

Priority order:

- keep `Mute`, `Select`, `Navigate`, `Pad Mode`, and `Solo` encoded as the current
  best-known pad-section mapping
- isolate `Pattern` and `Duplicate` next with the same low-fanout capture method
- leave the “first press may be missed” capture quirk alone unless it blocks future work

## Current Bottom Line

The `phys[0]` contamination bug is fixed in the `Debug` build.

Best current pad-section mapping:

- `Mute` -> `phys[17]`
- `Select` -> `phys[18]`
- `Navigate` -> `phys[20]`
- `Pad Mode` -> `phys[21]`
- `Solo` -> `phys[23]`
